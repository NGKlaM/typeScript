import React from 'react';

const LoginPage: React.FC = () => {
  return (
    <div>
      <h1>Login Page</h1>
      {/* Thêm mã HTML và trạng thái đăng nhập nếu cần */}
    </div>
  );
};

export default LoginPage;
